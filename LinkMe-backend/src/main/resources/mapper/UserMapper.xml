<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkme.backend.module.user.mapper.UserMapper">

  <resultMap id="BaseResultMap" type="com.linkme.backend.module.user.entity.User">
    <id column="user_id" property="userId"/>
    <result column="username" property="username"/>
    <result column="email" property="email"/>
    <result column="phone" property="phone"/>
    <result column="password_hash" property="passwordHash"/>
    <result column="nickname" property="nickname"/>
    <result column="gender" property="gender"/>
    <result column="created_at" property="createdAt"/>
  </resultMap>

  <select id="findById" resultMap="BaseResultMap" parameterType="int">
    SELECT * FROM User WHERE user_id = #{id}
  </select>

  <select id="findByUsername" resultMap="BaseResultMap" parameterType="String">
    SELECT * FROM User WHERE username = #{username} LIMIT 1
  </select>

  <insert id="insert" parameterType="com.linkme.backend.module.user.entity.User" useGeneratedKeys="true" keyProperty="userId">
    INSERT INTO User (username, email, phone, password_hash, nickname, gender, created_at)
    VALUES (#{username}, #{email}, #{phone}, #{passwordHash}, #{nickname}, #{gender}, NOW())
  </insert>

</mapper>
